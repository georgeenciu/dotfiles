#!/bin/bash

CODE_DIR=~/code
HOME_CODE_DIR=~/code/georgeenciu
BIN_DIR=~/bin
CONFIG_DIR=~/.config
TMP_DIR=~/tmp
SSH_DIR=~/.ssh
GNUPG_DIR=~/.gnupg

mkdir -p $CODE_DIR
mkdir -p $HOME_CODE_DIR
mkdir -p $BIN_DIR
mkdir -p $CONFIG_DIR
mkdir -p $TMP_DIR
mkdir -p $SSH_DIR
mkdir -p $GNUPG_DIR
